/*
let favouriteNumbers = [16, 32, 64, 96, 256];
console.log(favouriteNumbers[0], favouriteNumbers[2], favouriteNumbers[4]);

let favoriteFoods = [
  "Lasagne",
  "Ceilonesisches Reisfleisch",
  "Pizza",
  "Griesauflauf",
];

if (favoriteFoods.includes("Pizza")) {
  let index = favoriteFoods.indexOf("Pizza") + 1;
  console.log("Pizza befindet sich an Stelle " + index + " in der Liste.");
} else {
  console.log("Pizza befindet sich nicht in der Liste!");
}

let results = [12, 42, 1337];
console.log(results.join(", "));
let maxResult = results[0];
for (i = 1; i <= results.length; i++) {
  if (maxResult < results[i]) {
    maxResult = results[i];
  }
}
console.log(maxResult);
*/