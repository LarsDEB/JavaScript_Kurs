/*
let passwordTrue = ["geheim"];
let passwordGet;
do {
  passwordGet = prompt("Was ist dein Passwort?");
  if (passwordTrue.includes(passwordGet)) {
    console.log("Passwort korrekt");
  }
} while (!passwordTrue.includes(passwordGet));

let input = prompt("Gib eine Zahl zwischen 1 und 10 ein:");
while (!Number(input) >= 1 && !Number(input) <= 10) {
  input = prompt(
    input +
    " ist keine Zahl zwischen 1 und 10. Versuche es nochmal!",
  );
}
console.log('deine Zahl ist: ', input);
*/